<ul title="Carbon Diet" selected="true">
  <li class="group">Add Data</li>
  <% @current_user.electricity_accounts.current.each do |account| %>
    <li>
      <%= link_to("#{image_tag 'electricity.png'} #{account.name}", new_user_electricity_account_electricity_reading_path(@current_user, account)) %>
    </li>
  <% end %>
  <% @current_user.gas_accounts.current.each do |account| %>
    <li>
      <%= link_to("#{image_tag 'gas.png'} #{account.name}", new_user_gas_account_gas_reading_path(@current_user, account)) %>
    </li>
  </div>
  <% end %>
  <% @current_user.vehicles.current.each do |vehicle| %>
    <li>
      <%= link_to("#{image_tag 'car.png'} #{vehicle.name}", { :controller => "data_entry/vehicle_fuel", :action => "edit", :vehicle => vehicle.id } ) %>
    </li>
  <% end %>
  <li class="group">Analysis</li>
  <li>
    <%= link_to "#{image_tag 'chart_pie.png'} Totals", { :controller => "report", :action => "index" } %>
  </li>
  <li class="group">Help</li>
  <li>
    <%= link_to "#{image_tag 'help.png'} Help", "/help" %>
  </li>
  <li>
    <%= link_to "#{image_tag 'delete.png'} Log Out", "/user/logout", :target => "_self" %>
  </li>
</ul>
