<p>
<% @groups.each do |group| %>
<%= image_tag 'group.png' %>
<%= link_to h(group.name), group %> (<%= group.users.count %> members)
<br/>
<span class="smalltext"><em><%=h group.description.first(50) %>...</em></span>
<br/>
<% end %>
</p>
